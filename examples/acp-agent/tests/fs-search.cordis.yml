# Minimal live counterpart for the glob-sampling snapshot composition.
- id: llm-deepseek
  name: '@workspacealberta/wa-llm-deepseek'
  config:
    models:
      - id: deepseek-v4-pro

- id: subprocess
  name: '@workspacealberta/wa-subprocess-local'

- id: bash
  name: '@workspacealberta/wa-bash-local'

- id: acp-agent
  name: '@workspacealberta/wa-acp-demo'
  config:
    provider: deepseek-official
    model: deepseek-v4-pro
    persistenceRoot: !!js process.env.DSH_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
    persistenceCompression: !!js "process.env.DSH_SNAPSHOT === undefined ? 'zstd' : 'none'"
    workspaceContext: false
    skills:
      enabled: false
    toolJobs: false
    goals: false
    persona: You are a concise snapshot agent working in {{cwd}}.

- id: tool-fs-search
  name: '@workspacealberta/wa-tool-fs-search'
  config:
    sampleOverCapGlobResults: true
    globMaxResults: 4
