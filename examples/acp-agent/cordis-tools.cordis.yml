# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@workspacealberta/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@workspacealberta/wa-cordis-host-runner'
          - id: tool-cordis
            name: '@workspacealberta/wa-tool-cordis'
