# Keyless counterpart to session-query.cordis.yml: the nested snapshot overlay
# supplies replay plus deterministic private spill storage and its byte limit.
- id: base
  name: '@workspacealberta/cordis-plugin-include'
  config:
    path: ./fs.cordis.snapshot.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@workspacealberta/wa-tool-session-query'
          - id: timeout-policy
            name: '@workspacealberta/wa-tool-call-timeout-policy'
