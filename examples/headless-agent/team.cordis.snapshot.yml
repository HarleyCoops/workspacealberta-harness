# Keyless Agent Teams composition over the real headless app and deterministic fixture adapter.
- id: base
  name: '@workspacealberta/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@workspacealberta/wa-llm-deepseek'
        disabled: true
      - id: tool-subagent-control
        name: '@workspacealberta/wa-tool-subagent-control'
        disabled: true
      - id: tool-subagent-report
        name: '@workspacealberta/wa-tool-subagent-report'
        disabled: true
      - id: tool-subagent
        name: '@workspacealberta/wa-tool-subagent'
        config:
          provider: spawn
          toolName: subagent
          backgroundMode: one-shot
          maxDepth: 1
      - id: tool-subagent-fork
        name: '@workspacealberta/wa-tool-subagent'
        config:
          provider: fork
          toolName: subagent_fork
          backgroundMode: one-shot
          maxDepth: 1
      - insert:
          - id: agent-team
            name: '@workspacealberta/wa-experimental-agent-team'
          - id: tool-agent-team
            name: '@workspacealberta/wa-experimental-tool-agent-team'
          - id: team-fixture-llm
            name: './tests/fixtures/team-llm.mjs'
